# Ruff 0.16 expanded the default rule set from 59 to 413. This repo was
# written against the old defaults (pycodestyle E4/E7/E9 + pyflakes).
# Enabling the new set would be a style rewrite, not a CI gate.
[lint]
select = ["E4", "E7", "E9", "F"]
