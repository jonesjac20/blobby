# bots

Bot clients added in phase 6. Each bot will be an ordinary WebSocket client speaking the same `join` / `input` / `split` protocol as a human, with a simple decision loop instead of user input.
