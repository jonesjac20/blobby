# client

Browser client added in phase 3. Will be `index.html` + `game.js` (canvas render loop, mouse/keyboard input, WebSocket client) + `style.css`. Served as static files by the aiohttp server on the same port as the WebSocket endpoint.
